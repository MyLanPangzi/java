<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.histcat.springboot.jdbc.mapper.EmployeeMapper">
    <insert
            id="insert"
            parameterType="com.histcat.springboot.jdbc.entity.Employee"
            flushCache="true"
            statementType="PREPARED"
            keyProperty="id"
            keyColumn="id"
            useGeneratedKeys="true"
            timeout="20"
    >
        insert into testdb.employee (id, last_name, email, gender, d_id)
        values (#{id}, #{lastName}, #{email}, #{gender}, #{dId})
    </insert>
</mapper>